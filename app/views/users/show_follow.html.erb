<% provide(:title, @title) %>
<% content_for :showcase do %>
<div class="row">
  <div class="span8">
    <h3><%= @title %></h3>
    <% if @users.any? %>
      <ul class="users">
        <%= render 'users/user' %>
      </ul>
    <% end %>
	<% if @route %>
	  <%= link_to 'Back to route', @route%>	 
	<% else %>
	  <%= link_to 'Back to user', current_user %>
	<% end %>
  </div>
</div>
<% end %>