<% content_for :showcase do %>
<h1 class="new_route_header">New route</h1> 

  <% flash.each do |key, value| %>										
    <div class="alert alert-<%= key %>"><%= value %></div>			
  <% end %>
  
<%= render 'form' %>

  
<%= link_to 'Back', root_path %>

<% end %>
